{
  "name": "propwatch-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute propwatch-db --remote --file=schema.sql",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.9",
    "wrangler": "^3.0.0"
  }
}
